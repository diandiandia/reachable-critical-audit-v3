# BIAS_EVAL_V3_12 — v3.12 状态机分析能力补强 过设计 / 设计偏见 / 死代码 / 历史信息带入评估（2026-08-29）

> 评估对象：v3.12 全部新增机制（STATE 账本族 + 严重度映射 + 4 条 CK 清单 + 1 条 PREC）。
> 评估维度：过设计（机制超出需求/义务棘轮）、设计偏见（形态偏置/案例过拟合）、
> 死代码（定义无消费/死字段复制）、盲目带入历史审计信息（引用/案例未经实证队列核验）。
> 评估方法：全部裁决证据来自实证队列（verify_queue.json）与源码引用逐字核验，
> 不采信记忆转述。修复随 C0/P14-P17 提交。

## 1. 盲目带入历史审计信息评估（本版首要缺陷类）

| # | 发现 | 证据 | 判定 | 处置 |
|---|---|---|---|---|
| H-1 | 设计文档初稿称「状态机类 finding 此前**全落 OTHER**」（Pillow DCX/MPO、gson 无界 token 家族） | 实证队列核验：Pillow DCX/MPO = `cwe:["CWE-789"]`（→RESOURCE-DOS）；gson CAND-001/005 = `CWE-400`（→RESOURCE-DOS）。**无一条落 OTHER** | 事实错误（凭记忆转述） | ✅ 已修：S-A 问题域改「前瞻性锚点缺口」口径，历史 finding 实际落点如实注记 |
| H-2 | 将 gson 无界 token 家族列为 STATE 族案例支撑 | 同 H-1——该类属 RESOURCE-DOS（CWE-789/400），非状态机 CWE 类 | 类属错误 | ✅ 已修：从 STATE 案例支撑移除 |
| H-3 | 新清单 source_lessons 引用「W6 §18.9 状态机越权条款/多步流程不变式条款」 | 逐字核验：W6 §18.9 实际内容为「H1-F1 的 claim 迁移」（gate ③ 扩展至 R4 confirmed），**与状态机无关** | 虚构引用 | ✅ 已修：改为 CK-BIZ-LOGIC 既有步骤升级为族（v3.4 实际形态）+ Pillow 教训 |
| H-4 | 「严重度表无状态机 CWE→分级失真」暗示历史 finding 被误分级 | 实证：Pillow DCX/MPO 经 CWE-789 已正确得 High——无历史失真案例 | 案例支撑失真 | ✅ 已修：S-B 改为「841+claim=other 误入 medium 默认档」的误分级路径论证 + 全族入表惯例 |
| H-5 | Pillow 复盘引用（SKILL_LESSONS_Pillow.md:16「逐帧状态机 vs 逐帧门禁对账清单」） | 逐字核验属实 | 属实 | 保留（本版最强案例锚点） |

## 2. 过设计评估

| # | 发现 | 判定 | 处置 |
|---|---|---|---|
| O-1 | CK-STATE-CONFUSION 与 CK-STATE-TRANSITION **双锚 CWE-841**——每个 841 候选机械共绑 6 步（CONFUSION 的双路径解释/复用形态并非每个 841 候选具备） | 过设计（机械共绑膨胀；违背 CK-WS-MATERIALIZE 的「cwe 通用码须叠加问题域信号」教训） | ✅ 已修：CONFUSION 改无 CWE 锚定 + 关键词绑定（同 CK-FRAME-GATE-REENTRY 跨族形态） |
| O-2 | 新 CK 条目草案带 `"applications": []` 字段 | v3.4 批次遗留字段（无代码消费；新条目 CK-POSTOP-INVARIANT/CK-PINNED-DEP/CK-GENERIC-RESOURCE 均不带） | ✅ 已修：新 4 条不写该字段 |
| O-3 | 15 用例中 test_precedents_all_matchable_holds 复刻 test_v321 既有全局守卫 | 重复断言（test_v321:209-226 恒执行） | ✅ 已修：裁除，test_v312 为 14 用例 |
| O-4 | STATE 账本族 + 严重度映射 + 清单族 + PREC 的组合规模 | 克制（全部数据/映射层；绑定走既有 binder；注入走既有管线） | 不改 |

## 3. 设计偏见评估

| # | 发现 | 判定 | 处置 |
|---|---|---|---|
| B-1 | 案例支撑 Pillow 单点（帧/门禁词汇主导清单族与先例） | 案例过拟合的必然（同 v3.11 B-1 处置）：PREC 用 applicability_scope 声明适用域（多帧/多块/多记录容器）；清单条目机制形态书写；业务状态机侧由 CK-BIZ-LOGIC 共绑覆盖 | 记录待回填：下一在线项目验收自然检验，不做过度修正 |
| B-2 | 严重度档位判断 841/696→高（对齐 RACE）为主观档位选择 | 可辩护（行为工作流/顺序错误与竞态同属高后果类）；670→中为保守侧 | 不改（设计文档已注理由） |
| B-3 | CWE 集 {841,696,670} 排除 799 | 语义裁决（799=交互频率→RESOURCE-DOS 侧），非历史拟合 | 不改 |

## 4. 死代码评估

| # | 发现 | 判定 | 处置 |
|---|---|---|---|
| D-1 | 同 O-2：`applications: []` 死字段复制 | 死代码（数据层） | ✅ 已修 |
| D-2 | rows 空行 `{"family":"STATE","langs":{}}` | **非死**——缺口扫描（read 模式）只遍历 rows，无行则族不可见（batch_verify.py:1470-1477 实测） | 不改 |
| D-3 | KEYWORD_MAP 双键（状态机/state machine） | 均可达（CJK 与 ASCII 候选路径），test_v321 强制触达 | 不改 |
| D-4 | PREC `applications` 字段 | PREC schema 既有字段（16/16 全带；去项目化测试扫描 5 字段含之）——schema 一致性保留非死代码 | 不改（沿用既有 schema） |

## 5. 评估结论

- 盲目带入历史审计信息 **4 处事实/引用错误**（全修）——「落 OTHER」错误、gson 类属错误、
  虚构 W6 §18.9 引用、严重度失真暗示，均为凭记忆转述未核验实证队列所致。
- 过设计 3 处（双锚共绑/死字段/重复测试，全修）、死代码 1 处（同死字段，已修）。
- 偏见 1 处记录待回填（Pillow 单点案例过拟合——以声明适用域 + 下一在线项目验收处理），
  2 处可辩护裁决不改——评估本身遵守义务棘轮。
- 修复后回归：随 C0/P14-P17 提交，P17 全量回归全绿为判据。
