# BIAS_EVAL_V3_14 — v3.14 protobuf 复审计复盘缺陷修复 四项缺陷评估（2026-08-29）

> 评估对象：v3.14 全部新增机制（8 项修复 D-1~D-8）。
> 评估维度：盲目带入历史审计信息、过设计、设计偏见、死代码。
> 评估方法：案例支撑全部来自 2026-08-29 protobuf 复审计会话内实录（journal 输出/
> 命令行结果/手工操作记录），代码路径归属经 Explore 代理逐行取证核实——不采信
> 记忆转述。修复随 C0/P22-P25 提交。

## 1. 盲目带入历史审计信息评估

| # | 发现 | 证据 | 判定 | 处置 |
|---|---|---|---|---|
| H-1 | D-1 的「verify 模式也误报」初稿表述 | 取证纠正：`stage_collect` 从不调用 `_detect_journal_anomaly`，误报仅限 r35/r35n 两阶段（batch_verify.py:557/1282） | 初稿不精确 | ✅ 已修：REQ-V3.14-001 按取证修正语义（r35 传 2，r35n 默认 1） |
| H-2 | D-4 初稿称「无修复机制」 | 取证纠正：前缀归一化修复已存在（`_map_surface_id` batch_verify.py:876-892 + norm 去 SURF- 前缀），残缺口恰是 S-DATA-* vs SURF-DATA-* 跨前缀形态 | 初稿夸大缺口 | ✅ 已修：REQ-V3.14-002 措辞按残缺口收窄 |
| H-3 | D-6 的 H4-F5 矛盾归为 skill 缺陷 | 逐条核实：矛盾真实（H4-F5「维持 UNREACHABLE」vs CAND-009 终态 NEEDS_REVIEW，报告定稿前零拦截），但工件纠错属审计收尾，skill 缺陷=无机械检查 | 归因混合 | ✅ 已修：工件修正单列（随 P23），skill 缺陷只做 warn 检查 |
| H-4 | 8 项案例支撑的来源 | 全部为会话内可见事实（journal_anomaly 输出 2 次/IDEMPOTENT_SKIP+STATExc/sample 冲突/HYP-001 落盘/三处 unknown_surface_ids），代码行号经取证报告逐点确认 | 属实 | 保留 |

## 2. 设计偏见评估

| # | 发现 | 判定 | 处置 |
|---|---|---|---|
| B-1 | D-7 把自身编排便利当 skill 义务（4 次 UNWRITTEN 根因=主代理派发只读 Explore 代理，skill 从未强制；落盘拦截契约已覆盖失败形态） | 偏见成立 | ✅ D-7 降级为 SKILL.md 一行指引，不做 SWR 机制 |
| B-2 | D-2 修复幅度诱惑（手工合并之痛→倾向自动 re-credit 大修） | 义务三问：复审计是设计工作流（R2 复审计注入条款在册），账本无复审计语义是真实不一致；但自动合并有双计数风险，单案例不支撑自动语义 | ✅ 最小幅度：幂等分支结构化增量指引 + 手工合并协议；第二个复审计案例再评自动 re-credit |
| B-3 | D-4 自动修复诱惑（close-miss 可机械映射→想直接改 tracked_surfaces） | 误猜风险>收益（近似匹配可能错配合法 id 变体），手工修复三处只需一次裁决 | ✅ 只做 suggested_corrections 提示 |

## 3. 过设计评估

| # | 发现 | 判定 | 处置 |
|---|---|---|---|
| O-1 | D-6 做成新门禁的诱惑 | 六门禁判据语义不变是版本铁律；字段级 warn 足够拦截（消费者=主代理复核） | ✅ warn 字段，不加 gate |
| O-2 | D-3 把 sample 文件变强制义务的诱惑 | 文件可选=零新义务；不存在时内部抽样即默认路径（现状） | ✅ 读-写顺序修复，文件保持可选 |
| O-3 | D-1 参数化后 r35n 传参需求 | N=1 是设计形态，默认值 1 已覆盖——不传参不写死 | ✅ 默认参数 |
| O-4 | 8 项修复组合规模 | 全部小/极小改动（1 参数+3 告警增强+1 读序+文案×2+指引×1），无新资产族 | 不改 |

## 4. 死代码评估

| # | 发现 | 判定 | 处置 |
|---|---|---|---|
| D-1 | D-3 改后导出器内部抽样 `resurrect_pool`（workflow_export.py:440-461） | **非死**——文件不存在/不一致时是缺省路径 | 不改 |
| D-2 | 新增代码无孤立符号（suggested_corrections 由告警消费者读取；r3_link_invalid/r4_verdict_link_conflict 由主代理复核消费） | 无死代码 | 不改 |
| D-3 | `_resurrect_sample.json` 读取方新增后文件不再是 write-only | 消除现存 write-only 死形态 | 修复本身即消死 |

## 5. 评估结论

- 盲目带入 2 处初稿不精确（D-1 模式范围、D-4 缺口夸大）经取证纠正、1 处归因混合拆分、
  8 项案例支撑全部实录核实。
- 设计偏见 3 处成立（D-7 降级/D-2 最小幅度/D-4 只提示）——本版最重要的评估产出是
  「不做」清单。
- 过设计 2 处克制（无新门禁、文件可选）+ 1 处默认参数。
- 死代码：修复本身消除 write-only 文件形态，无新增死资产。
- 修复后回归：随 C0/P22-P25 提交，P25 全量回归全绿 + protobuf 受影响阶段复跑零回退为判据。
