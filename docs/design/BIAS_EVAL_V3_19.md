# BIAS EVAL V3.19 — 四缺陷评估（2026-09-02）

## ① 盲目带入历史审计信息

- **案例支撑**：全部六条缺陷指向 /root/v8/.audit_results/lessons.md 具体条目
  （"对 skill 的教训"×2 + "实证复活波追记·对 skill 的教训"×4），会话内实录。
- **去项目化**：运行时资产（evidence_ledger 注释/_build_prompt 措辞/
  resurrect_prompt 第 9 维/ENVIRONMENT_PROBES 条目/SKILL.md 条款）零项目名——
  机制形态（"快照 blob 实证实录"类措辞走追溯字段或省略）；测试守卫
  DEPROJECT_BLACKLIST 扫描 ENVIRONMENT_PROBES 新条目。
- 审计自身教训三条（harness 编译链/对照组方法论/checksum 校准）经义务三问
  无明确运行时消费者——不入运行时资产, 留项目 lessons（诚实边界声明）。

## ② 设计偏见

- **不自动改写**：D-1 lenient 是跳过（零改写断言进测试反面分支）；
  D-2/D-3/D-6 全部提示级；D-4 只明示既有契约。
- **编排便利 ≠ skill 义务**：D-3 的"优先实证"是提示不是强制优先级
  （不复制批次选题强制语义）。
- **修法层级**：agent 行为偏差（verifier 分歧类）以任务书措辞承载
  （v3.15 D-6/D-8 先例：容忍契约优先）。

## ③ 死代码

- 无新函数、无新字段、无新资产——六条修复全部落在既有代码路径的
  措辞/容错修改。消费者逐项：D-1（assert_ledger 检查路径）、D-2（verify
  prompt 消费）、D-5（R5 探针消费）、D-6（resurrect prompt 消费）、
  D-3/D-4（主代理裁决流程消费）。

## ④ 过设计

- 无新门禁名、无新阶段、无新强制义务、无新机制。
- 义务入库三问逐项：D-1 触发=检查路径遇 str 条目（放宽兼容, 零新义务）；
  D-2 触发=库型目标 verifier 任务书（提示级）；D-3 触发=抽样/裁决时（提示级）；
  D-4 触发=实证降级动作（明示既有契约）；D-5/D-6 触发=实证环境/复活任务书
  （内容条目）。
- **裁除执行**：L4 方向的原机制（resurrection_review 落盘契约 v3.2.2 +
  gate ③c）不重造；审计自身教训三条不入库（无消费者）。
- **明确不做**：不建"claim=other 强制实证"义务（实证豁免是 v3.3 明示条款,
  本版只补提示引导）；不改 gate ③ 判据；不把构建矩阵枚举变成复活波强制步骤。

## 实现期复查计划

lenient 零改写断言 / 新资产 DEPROJECT 扫描 / 门禁名 grep 零新增 /
400 基线零回退 / V8 队列复跑 warn 逐条一致。
