# BIAS EVAL V3.22 — 四缺陷评估（设计期，2026-09-04）

> 实现期必须重跑（v3.16 教训：设计件零违规≠实现零违规）。

## ① 盲目带入历史审计信息

- **案例支撑**：全部缺陷指向 /root/firefox/.audit_results/lessons.md
  §一.1/4/6 + §一补.7/9/10/11/12/13（2026-09-04 会话实录 + 同日补记）
  与 WebKit lessons §一.1（D-10 吸收）——零凭记忆转述。
- **数字核实**：3 例截断自愈（CAND-014/015/017）、34 例 override、
  111/111 覆盖、payload 104KB→8KB 均本会话机械产物实测。
- **去项目化**：运行时正文（代码注释/模板条款/SKILL.md 条款）零项目名、
  零候选 id——案例只进设计件与追溯字段；教训归属写 lessons 文件。
- 机器守卫：不新增计数资产; PROJECT_TOKENS 扫描既有守卫覆盖。

## ② 设计偏见

- **不自动改写**：D-2 是确定性渲染规则（claim=other 为候选自报事实）;
  D-4 只补缺席簿记（幂等, 不覆盖既有）; 测试含"已有簿记不覆盖"反面。
- **编排便利 ≠ skill 义务**：D-9 薄封装是派发效率形态不是强制义务
  （内联回退保留）; D-7 决策签入是问责契约（用户决策权条款的自然延伸）。
- **修法层级**：D-3 取证裁除（主代理操作失误 ≠ skill 缺陷——容忍契约
  优先）; D-6 落盘契约对齐 R1/R2 既有形态（不发明新契约）。

## ③ 死代码

- **零新函数**：D-1/D-2/D-4/D-5 全部在既有函数内改动; D-9 复用既有
  落盘模式。
- **新字段消费者**：auto_bookkept → collect 结果 JSON（主代理对账）;
  decision → skill-optimizer 复盘问责。
- **顺手清理**：无已证实死代码涉及本版编辑区。

## ④ 过设计

- **无新门禁名/无新阶段/无新强制义务**：全部机械规则/提示级/条款级。
- **义务三问**已逐项过（REQ 表六项）。
- **取证裁除**：D-3（既有机制, 双形态容忍在位）、WebKit §一.2 tracked-ids
  改名（语义已内化, 改名多消费方风险>收益）——两例裁除均为标准动作。
- **排队声明**：Firefox §二（审计自身教训）与 §一.2/3/5（正向确认）
  不入运行时; 召回率回归集（上一轮战略欠账）仍排队待用户裁定启动。

## 实现期重跑清单

- [ ] 新条款/注释零项目名（grep 断言）
- [ ] D-4 幂等与不覆盖反面真跑
- [ ] D-2 对 Firefox 真实队列复跑零新增告警 + 渲染降为 medium 与
      既有 override 一致性
- [ ] 旧队列（WebKit/v8）assert_ledger 零新增
