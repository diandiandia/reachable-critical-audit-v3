# BIAS_EVAL_V3_23 — 四缺陷评估（设计期）

## ① 盲目带入历史审计信息

- 案例支撑全部为会话内实录：H3-F1（Firefox lessons §三.1，real-target 探针数字
  1033/752 零释放）、85046（v8 lessons 召回复盘 1，2026-09-06 追加）、WebKit 二.1
  （R4/R2 冲突实录）。零凭记忆转述。
- **去项目化检查**：所有运行时正文（SKILL.md 条款、模板段、fixture description）
  零项目名/零 CAND- 号；`case_source` 为追溯字段（允许项目名，纪律允许）。
- 技术名保留判定：`maglev`/`turbofan` 是 JIT 编译器通用技术名（Google 开源项目
  技术词汇），非项目名——与 `wasm`/`zlib` 同属技术词汇层，不违反去项目化。
- 实现期将重跑本项（机器守卫 test_deproject_assets 若新增正文带项目 token 需扩守卫）。

## ② 设计偏见

- 无编排便利转义务：D-5 明确不建运行时度量工具（"先语料后工具"——消费者不足）；
  D-4 为提示级可选通道（义务三问：触发条件=profile 信号，消费者=R2 假设生成，
  案例=85046）。
- 无自动改写：D-3③ 信号只进 `recommends`；D-1 不自动改 harness 产出（提示级）。
- 修法层级正确：全部为条款/模板/提示（P2-P3），无 agent 行为偏差被硬编码为义务。

## ③ 死代码

- 本版零新函数；D-3③ 新增的信号检测为纯函数，消费者 = target_profile.py 建议
  输出（既有消费者链完整）；D-5 fixture 的消费者 = skill-optimizer 阶段 0 +
  阶段 6 判据（文档化引用）。
- differential_probe.py 零逻辑改动（仅 docstring）——无死代码风险。

## ④ 过设计

- 零新门禁名、零新强制义务、零新阶段（SWR 共性约束已写死）。
- 义务三问逐项过（见 ②）。
- 阶段 1 取证裁除 3 项已存在机制（v3.19-003/005/006）——防重建先例动作。
- D-5 的"度量工具"诱饵被主动拒绝（过设计防线标准动作）。

## 实现期重跑项（阶段 3 完成后必查）

- 新正文零项目 token（跑 test_deproject_assets + 人工扫）
- SKILL.md 增量段数字与先例/清单条数守卫一致
- 版本守卫测试全绿（test_v310/v312/v313/v39/v314/v315/v3210/v322 等含版本断言处）
