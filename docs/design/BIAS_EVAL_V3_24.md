# BIAS EVAL V3.24 — 四缺陷评估（设计期；实现期必须重跑）

## ① 盲目带入历史审计信息

**检查点**：
- 案例支撑全部为会话内实录（三引擎 classify 文件逐条 + recall_eval 数字 +
  firefox lessons §三.2），来源字段已在 REQ 缺陷清单逐条标注——无凭印象
  转述（开发全程随时回读对应条目核对）；
- **去项目化**：五处插入文本（D-1/D-2/D-3/D-5 运行时文本 + D-4 fixture
  条目除 case_source 外）设计期逐句预审零项目 token；机器守卫：
  test_v324.py 内置 PROJECT_TOKENS 扫描新增文本段（历史教训：v3.23 设计件
  零违规、实现期仍把案例名写进正文被 test_v323 捕获——实现期重跑必做项）；
- RECALL-002..004 的 defect_class/expected_hypothesis 为通用机制形态
  （无引擎/组件专名），family/cwe 用标准族。

**结论**：设计期无违规；实现期逐段扫描重跑。

## ② 设计偏见

**检查点**：
- 是否把自身编排便利当 skill 义务：否——D-4 fixture 仅测试/评估引用
  （阶段 0/6 对照消费），未建运行时度量工具（v3.23 D-5 同裁决先例）；
- 是否自动改写：否——五修复全部提示级/warn 注记，无自动改判/重写逻辑；
- 修法层级：agent 行为偏差不上升 skill 硬义务（D-1/D-2 为任务书提示级
  锚点，D-5 为流程提示句，D-3 为声明文档）——容忍契约优先。

**结论**：无违规。

## ③ 死代码

**检查点**（每个新文本列出消费者）：
- D-1 子条 → 消费者 R4 H4 假说子智能体（任务书注入）；
- D-2 条款 → 消费者 R2 假设生成 + R3 验证判定；
- D-3 声明 → 消费者 R2 假设生成边界 + 报告渲染段（341-344 行原文被
  --stage report 引用）；
- D-4 fixture → 消费者 test_v324 守卫 + 阶段 0/6 对照（v324_requirements
  已声明用途）；
- D-5 提示句 → 消费者 R5 实证阶段主代理 + 报告 fidelity 呈现。

**结论**：零死文本；本轮零新函数/字段，无需清理项。

## ④ 过设计

**检查点**：
- 零新门禁名、零新阶段、零新义务字段（REQ/SWR 已声明）；
- 义务三问逐项回答（SWR 各条）：
  - D-1：触发=H4 运行目标/消费者=H4 子智能体/裁掉丢 site-isolation 系统性盲 ✓
  - D-2：触发=含 jit 目标/消费者=R2+R3/裁掉丢族边界失真 ✓
  - D-3：触发=无条件声明/消费者=R2+报告/裁掉丢两族排除锚点 ✓
  - D-4：触发=仅测试引用/消费者=守卫+对照/裁掉丢三族对照力 ✓
  - D-5：触发=equivalent 档+真实目标可及/消费者=R5 主代理/裁掉丢 DDL 断链 ✓
- 取证裁除：C-1..C-4 四项全部经义务三问/取证裁决（REQ 表），无已存在
  机制的重复建设（D-5 已核实现有 fidelity 段无抽验句）。

**结论**：无违规。

## 实现期重跑清单（阶段 3 完成后必做）

1. test_deproject_assets 全量跑（含 test_v324 新扫描断言）；
2. 逐段 grep 新插入文本核对零项目 token；
3. 对照 REQ 案例支撑条与 classify/lessons 原文数字一致；
4. 版本 sed 后 grep `SWR-V3.23` 串数量与变更前一致（历史 ID 零变化）。
