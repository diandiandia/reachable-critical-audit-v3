# BIAS EVAL V3.25 — 四缺陷评估（设计期；实现期重跑）

## ① 盲目带入历史审计信息

- 案例支撑全部为本会话 v3.24 验收审计实录（MaintainWise lessons 5 条 +
  用户追问取证裁决）——REQ 缺陷清单逐条标注来源 ✓
- 去项目化：D-1/D-2/D-4 为机制代码（零项目名）；D-3 插入文本（编码矩阵
  条款）与 D-5（预置数据文件面）与 D-6（核取提示句）预审零项目 token;
  test_v325 内置 PROJECT_TOKENS 扫描守卫（防实现期回退, v3.23 教训）
- 用户追问的 4 项裁剪候选经取证裁除 3/降级 1——本周期未把"观察到的执行
  层冗余"当机制缺陷带进来（上一轮教训的直接应用）

## ② 设计偏见

- 是否把自身编排便利当 skill 义务：否——D-1/D-2 的案例是 Workflow 基础设施
  故障+args 限额的机制级实录, 非个人偏好; D-6 为提示级一句话, 不建义务
- 是否自动改写：否——D-4 只 warn 建议, severity_override 仍主代理裁决
- 修法层级：P1 机械（D-1 代码缺省形态）→ P2 结构（D-2 通道契约/D-4 warn）
  → P3 内容（D-3/D-5/D-6）——无 agent 行为偏差上升硬义务

## ③ 死代码

- D-3 `_is_path_family` 判据内联（不建无消费者的 helper）——义务三问③；
- D-4 `_sev_rank` 用内联 dict 不建函数；
- D-2 的 A' 装载为 r35-collect 内分支（消费者=机械多数决, 同一路径）；
- 零新字段、零新函数落库外。

## ④ 过设计

- 零新门禁名/新阶段/新义务字段（六 SWR 全部提示级/warn/默认形态变更）；
- 义务三问逐项在 SWR 各条回答 ✓；
- 取证裁除：用户追问 4 候选中 3 项已存在机制/活消费者（CK 交叉引用注释、
  lessons_recorder 活写路径、retire_low_contribution 活消费者）——本周期
  零重复建设。

## 实现期重跑清单（阶段 3 完成后）

1. test_v325 + 全量回归 + test_deproject_assets 全绿；
2. D-3 插入文本零项目 token grep 核对；
3. D-1 导出后 payload 大小断言（taskFile 引用形态）；
4. 版本 sed 后 `SWR-V3.24` 历史串计数与改前一致。
