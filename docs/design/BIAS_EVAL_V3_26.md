# BIAS_EVAL_V3.26 — 四缺陷评估（设计期）

## ① 盲目带入历史审计信息

- 本周期缺陷全部来自**本会话取证实录**（行号实测/grep 输出/git log 实证）：
  D-1 = git status 实录 + dev/installed diff 实录；D-2 = `ls tools/` 实录 +
  SKILL.md:980；D-3 = grep "TOOLING 3.25" 全文零命中实录 + SKILL.md:979
  历史实录；D-4 = git diff 内容核读（合法覆盖账本数据）；D-5 = 三处权威
  路径取证 + git log 单次触碰实录。零凭印象。
- 去项目化：本周期零新资产、零项目名入运行时正文；SKILL.md:980 标注仅提
  兄弟 skill 名（非审计项目名，归属注记允许位）。
- 机器守卫：test_deproject_assets.py 无需扩展（无新资产）。

## ② 设计偏见

- D-1 拒绝级守卫的依据是漂移复发史实证（v3.16 warn → v3.22 复发），
  非"自身编排便利"；豁免通道（--allow-dirty）防误伤开发期快速安装。
- 无自动改写：不自动 commit/stash，拒绝+指引。
- 修法层级正确：D-2/D-3 属规格-实现一致性，落测试层而非运行时义务层
  （不新增审计流程义务）。

## ③ 死代码

- 新函数消费者：`check_git_clean` → install.sh 主流程直调（test_v326 三用例
  消费）；test_doc_lint 两新用例 → pytest 消费。
- 顺手清理：D-5 根 REQUIREMENTS_TRACKING.md 即已实证死文件（零消费者）。

## ④ 过设计

- 无新门禁名、无新阶段、无新强制审计义务、无新资产——全部为 dev 侧交付
  守卫与测试。
- 义务三问逐项过（见 SWR_V3_26.md 义务三问节）。
- 反思记录：本周期拒绝在 P0 内顺带实施"规格升版（28 增量段整合）"——
  该事项属要件级，改动量与风险远超交付链修复，留待独立周期裁决
  （过设计防线：不把清单上的问题一次全变成待办）。

## 实现期重跑（阶段 7 义务）

实现完成后对照本文件逐条重跑一遍，特别是：
- SKILL.md:980 标注文本不得引入审计项目名
- install.sh 拒绝消息不得硬编码绝对路径（环境纪律 #8：默认 DST 既有
  形态不动，消息用变量）
- test_v326 fixture 内不得出现真实项目名
