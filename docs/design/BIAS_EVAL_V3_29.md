# BIAS_EVAL_V3.29 — 四缺陷评估（设计期）

## ① 盲目带入历史审计信息

- 试点条目唯一来源=WebFetch 取证的 2025 CWE Top 25 真实榜单(本会话取证
  实录,URL 逐条可复核);title=CWE 官方名,零转写
- 语言映射=设计件记录的保守适用性判定(SOFTWARE_DESIGN P2 表),非"凭印象
  种格"——每条可复核、可反驳
- 零项目名:pattern/pitfall 为通用机制描述;origin(追溯字段)只含权威源 URL
- 机器守卫:test_v329 正文键 deproject 断言 + test_deproject_assets 矩阵扫描
  (双写新增 cells 必须过)

## ② 设计偏见

- hitrate 只读零改写;seed 幂等;条款全部提示级(零强制义务)
- 映射表只做数据不做机制(不建"cwe→语言自动映射器"——适用性判定需要
  领域判断,自动映射是误猜风险>收益,同 v3.14 D-4 裁除先例)

## ③ 死代码

- priority 消费者=主代理选题/种格决策;hitrate 消费者=收官复盘;
  双写消费者=一致性测试;条款消费者=R2/R6 流程
- 无新函数无消费者形态

## ④ 过设计

- 零新门禁、零流水线改动、K1 达标无自动阻断
- 不做"自动种格"(seed 显式命令,人控数据入口)
- 不做"命中率自动入账本"(提示级条款,落 lessons 过程观察段即可——
  义务三问②无机械消费者,防指标棘轮)

## 实现期重跑（阶段 7 义务）

- 156 条目落盘后:正文键 grep 零项目名;origin 全带 URL;verify 全 unverified
- 矩阵双写后:34 cells → 新增格统计;test_deproject_assets 全过
- 守卫 ×19 全同步;K1 16/16 由 goal 命令实测输出
