# BIAS_EVAL_V3.31
① 案例支撑全部会话内实录（QuickJS 各点），零凭记忆；项目名零入正文 ✓
② 不自动改写：①b 只阻断不修正；D-5 只 warn ✓
③ 新参数/新命令消费者：assert_ledger 调用点（SKILL.md 代码块+stage assert 传 None）、
   hints（R2）、write_lesson（R6）✓
④ 无新门禁名（①b 属 ① 子检查）、无新强制义务（D-5 warn）、不建自动执行器 ✓
实现期重跑：QuickJS 队列复跑零新增 blocking；install 后残留 SKILL_LESSONS_quickjs.md 被同步删除。
