# BIAS_EVAL_V3_34: 四缺陷评估 (分层重构)

## ① 盲目带入历史审计信息
纯结构变更, 零资产正文变更 (git mv 验证内容逐字节不变)——无新正文可带项目名。
test_deproject_assets 全绿 (扫描基座改 ASSETS_DIR 后仍全量覆盖资产层)。

## ② 设计偏见
分层方案来自职责分析而非编排便利: L1 机制/L2 编排/L3 数据的划分判据是
"被谁 import/被谁读/是否随版本演进独立", 非按文件数或历史习惯。
唯一例外边 (workflow_export→batch_verify) 显式论证并注明注销条件。

## ③ 死代码
无新函数; _paths.py 三个常量全部有消费者 (7 模块派生 + 后续模块)。
lessons_recorder 的 LESSONS_DIR 保留 (战役归档读入口, 语义未变)。

## ④ 过设计
零机制新增、零门禁变化、零新义务——纯移动+派生修复。
分层文档 (FILE_LAYOUT) 是用户明确要求的交付物 (非义务棘轮)。
