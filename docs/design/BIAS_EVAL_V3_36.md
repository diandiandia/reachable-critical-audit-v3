# BIAS_EVAL_V3_36: 四缺陷评估

## ① 盲目带入历史审计信息

- 案例支撑为会话内实录 (Caddy 合并产物逐面 {type,original} 差异统计, 35/70);
- 修复代码与设计件零项目名 (caddy 仅出现在追溯/案例引用);
- 去项目化机器守卫 (tests/test_deproject_assets.py) 不涉及新增资产——本周期
  零新资产入库, 守卫免扩。

## ② 设计偏见

- 本周期修复的**正是**自动改写 (关键词映射误伤规范值)——修法方向为
  "精确值透传 + 自由文本保留既有映射", 无新的自动改写引入;
- 无编排便利义务 (义务三问已过, 见 SWR);
- 修法层级正确: 归一化数据正确性是运行时机制缺陷, 不是 agent 行为偏差——
  属 P1 机械层。

## ③ 死代码

- 新增两处短路均为既有 normalize 路径的分支, 消费者 = validate/merge 全链
  (R1 每批必走), 无死代码;
- 无新函数/新字段引入。

## ④ 过设计

- 无新门禁名、无新强制义务、无新阶段、无新 CLI;
- 无新资产;
- 修复是纯数据正确性还原 (规范值本不应被映射), 不扩大任何义务面。
