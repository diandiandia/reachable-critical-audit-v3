# BIAS_EVAL_V3_38: 四缺陷评估

## ① 盲目带入历史审计信息

- 案例支撑全部会话内实录 (CAND-001/002/005/011 波次产物);
- 新条目/条款正文零项目名 (Caddy 仅进 source_lessons 追溯字段; T-1/T-3 断言);
- 语言无关性已单独论证 (用户问询答复 + 评估件第三节): 机制层零语言语义,
  A 措辞中性化 ("同功能族兄弟实例" 而非框架词)。

## ② 设计偏见

- 无编排便利义务 (五机制全部有发现力因果链, 见评估件);
- 无自动改写 (全部提示级/清单级, 不触碰裁决机制);
- 修法层级正确: D-0 数据缺陷 (P1); D-2 schema+条款 (P2); 其余内容层 (P3)。

## ③ 死代码

- 每个新字段/条款的消费者已列 (SWR 各条): self_refutations → refute_prompt
  注入 + 报告佐证; binding → binder 注入; SKILL.md 条款 → 假设生成/复盘读入;
- 无新函数 (仅 prompt 组装段追加文本 + schema 字段)。

## ④ 过设计

- 零新门禁/零新强制义务/零新阶段/零新 CLI (SWR 兼容性不变式);
- 义务三问逐项过 (SWR 各条);
- 评估件裁除 F/G 两候选 (动态证伪者数量/批内教训注入) 记录在案。
