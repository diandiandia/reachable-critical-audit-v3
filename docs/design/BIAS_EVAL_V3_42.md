# BIAS_EVAL_V3_42

四缺陷评估（设计期版，实现期须重跑）。

## ① 盲目带入历史审计信息

- 全部 6 条缺陷的案例支撑来自 Keycloak lessons.md「对 skill 的教训」节
  具体条目（教训 1/2/3/4 各对应 D-3/D-4/D-5/D-6）+ 本次会话内实录
  （两次导出空转 + 手工清理 12 个空 dict → D-1；两次 R4_COLLECT_WARNING
  手工定位 → D-2）
- 设计件案例引用零项目名/零框架名（「CAND-010 isAbsolute 子断言被
  real-target 证伪」为追溯形态，不含目标名）
- 无历史项目目录/API 名进入运行时资产

## ② 设计偏见

- 修法全部提示级/判定健壮化，零自动改写
- D-1 修在判定健壮化而非「签收脚本规范」——签收脚本是主代理行为，
  机制容忍契约优先
- 无把编排便利当 skill 义务的条款

## ③ 死代码

- 新函数 `_has_refutation_result` 消费者：export_script refutation 分支
  资格判定 + 空转 advisory（两处，同一函数）
- `empty_refutation_keys` 消费者：导出结果 advisory（主代理读）+ 测试断言
- 无其他新字段/新函数

## ④ 过设计

- 无新门禁名、无新阶段、无新强制义务
- D-3 提示级（不加重 verifier 义务：标注为提示而非必填字段）
- D-4/D-6 是 SKILL.md 条款补句（提示级），非新机制
- 义务三问逐项过：
  - D-1 触发条件=导出 refutation 波时；消费者=主代理派发；裁掉丢=静默空转
    重现（本次实录）
  - D-2 触发条件=r4-collect 零提取时；消费者=主代理定位；裁掉丢=手工读文件
  - D-3 触发条件=verifier 写证据时；消费者=主代理 R5 实证规划；裁掉丢=
    分支级断言误导实证路由（本次实录）
  - D-4 触发条件=upstream recon 时；消费者=R2 假设生成；裁掉丢=时间差
    发现漏报（本次 H-4-F1 High 实录）
  - D-5 触发条件=filter drop 裁决时；消费者=筛选质量；裁掉丢=依赖 R4 兜底
    补盲（本次 H-5-F1 实录）
  - D-6 触发条件=主代理实证回填时；消费者=gate 断言预判；裁掉丢=回填试错
    （本次 ③d 违规→补 independent_review 实录）
