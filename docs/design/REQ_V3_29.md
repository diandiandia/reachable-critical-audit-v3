# REQ_V3.29 — 闭环接线:控制器/执行器/反馈信号（2026-09-08）

> 本周期触发：用户两连追问——"v3.28 是否没有提高发现能力"(确认:是,
> 且语言覆盖率也未增加——36 条目系同一知识重组)+"工程控制论视角本次修改
> 是否需要"(确认:需要,但只建了变送器与设定值,控制器/执行器/输出反馈缺位)。
> 本周期把回路接完:gap→优先级(控制器)、试点种格(执行器)、命中率(输出
> 反馈)、R2 接通(执行器→被控对象)、seed 双写(传感器一致性)。
> 性质:加载器增量 + 数据周期(真实权威源试点种格)+ P3 条款。流水线/门禁/
> 裁决层零改动。

## 缺陷清单（代码核实）

| # | 缺陷 | 案例支撑 | 修复 | 编辑点 |
|---|---|---|---|---|
| D-1 | 控制器缺位:goal 视图有误差信号(gap)无消费端——种格/选题优先级不读 gap | v3.28 goal 输出纯展示;用户追问实录 | goal 输出增 priority 段:per_lang 按 gap 降序 + 选题建议(误差最大语言优先) | language_issue_matrix.py goal_progress |
| D-2 | 执行器空转:seed 通道零内容(K1 0/16) | v3.28 验收实录 | 试点种格:2025 CWE Top 25 真实权威源(WebFetch 取证,16 语言 × 榜单映射 156 条目,全部 origin 带 URL+rank)——经自家 seed 命令入库(机制自举) | resources/language_issue_inventory.json 数据 |
| D-3 | 输出反馈缺位:库存指标(格数)≠火力指标(命中率)——资产增强不传导到发现能力的度量不存在 | 用户追问核心 | hitrate CLI:`hitrate <lang> <cwe,...>` 计算审计确认问题 cwe 命中该语言条目数;R6 收官条款(提示级):每场审计记录命中率 | language_issue_matrix.py + SKILL.md 增量段 |
| D-4 | 资产层→发现层未接通:inventory 排序条目不进 R2 假设空间 | v3.28 P1(用户追问确认) | R2 条款扩展:假设生成前读该语言 cells **与 inventory 排序条目**(提示级,SWR-V3.18-002 语义扩展) | SKILL.md R2 段(增量段) |
| D-5 | 双资产漂移风险:inventory 与矩阵两套传感器 | v3.28 P3 | seed 双写:入库 inventory 同时更新矩阵 cells(缺格建格/补 pattern/并 cwe/补溯源);一致性测试(inventory 条目 ⊆ 矩阵格 pattern) | language_issue_matrix.py seed_entries + test_v329 |

## 试点种格数据规范（诚实纪律）

- 来源:2025 CWE Top 25(cwe.mitre.org/top25/archive/2025/2025_cwe_top25.html,
  WebFetch 取证)——origin 字段带 URL + rank;date=2026-09-08
- tier=external_seeded;verify=unverified(未战役验证,升档靠回填纪律)
- 语言映射为保守适用性判定(设计件记录映射表,可复核);pattern/pitfall 为
  通用机制形态描述,零项目名
- 预期:156 条目 → inventory 36→192;K1 16/16;K2 维持 0/16(诚实)

## 义务入库三问（逐项）

- D-1:①触发=goal 命令显式调用;②消费者=主代理选题/种格优先级决策;③裁掉丢什么=误差信号无消费端(用户追问实录)。
- D-2:数据周期,无义务。
- D-3:①触发=审计收官;②消费者=复盘/资产有效性判定;③裁掉丢什么=资产→能力传导无度量(用户追问核心)。
- D-4:①触发=R2 假设生成;②消费者=假设空间;③裁掉丢什么=知识不进假设空间(v3.28 P1)。
- D-5:①触发=seed 命令;②消费者=两资产一致性;③裁掉丢什么=双传感器漂移(v3.28 P3)。

## 修法形态纪律

零自动改写(seed 显式/幂等;hitrate 只读);零新门禁;P3 条款提示级;裁决层零改动。

## 最小实验（本周期执行,Phase 3.29 验收判据）

K1 达标后(16/16),对 C 语言执行对照检查:①inventory c 排序条目含 Top25 条目;
②hitrate 对 QuickJS 确认问题 cwe 集(770/787/125/269/672/770/693/22/94/862/284/749/835/285/639)计算
命中条目数——预期 ≥3 命中(770/787/125 均有条目),证明"资产→能力"通道在
语义上可达。发现能力实证仍以下一场真实审计为准(QuickJS 复跑 R2 对照留待
下一验收项目)。

## 验收判据（Phase 3.29）

test_v329 新增用例全绿 + 全量回归全绿 + install 双副本同步 + K1 16/16 +
seed 双写一致性断言 + 去项目化扫描零命中。
