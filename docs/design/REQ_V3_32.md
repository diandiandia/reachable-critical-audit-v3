# REQ_V3.32 — 发现力四杠杆（2026-09-08）

> 触发：用户问"如何依赖模型能力增强漏洞发现力，现有设计是否可以优化"→
> 四提议经通用性检验(第一原则三禁止全过)+因果链论证(每条对应战役观察)
> +可证伪判据确认后，用户裁定开做。
> 性质：提示级/数据级——零流水线/门禁/裁决层改动。

## 缺陷清单（代码核实）

| # | 缺陷 | 案例支撑 | 修复 | 编辑点 |
|---|---|---|---|---|
| D-1 | R2 假设空间缺"缺陷形态"输入维度——面图→假设 keep=0 与族深挖→假设 7/9 的对照实验 | QuickJS 战役实录 | R2 对抗枚举条款(P3)：hints 装载后对每族 Top 条目问"该缺陷形态在本目标可能藏在哪些 sink 形态" | SKILL.md R2 段 |
| D-2 | 假设生成靠模型通用记忆，无证据锚定——修复 commit 是目标缺陷形态 ground truth 却未系统挖掘 | 修复变体复核先例(SKILL_LESSONS_C §1.4.5)+ QuickJS 上游窗口证据 | tools/fixminer.py：git log 安全修复挖掘(关键词分级：security 词 vs 通用 fix 词)+族分类输出(提示级)；R2 条款引用 | 新工具 + SKILL.md |
| D-3 | 证伪者 sibling 向量捕获靠主代理自觉(三次高价值发现，无管道保证) | CAND-003 系证伪者产出实录 | r35-collect 结果回显 strengthened + sibling 裁决提示(SWR-V3.19-003 实质机制优先) | tools/batch_verify.py |
| D-4 | 知识可达性差：lessons 在硬盘≠在上下文；hints 不随目标形态适配 | QuickJS R2 时 30+ lessons 零装载实录 | hints 增 --kind 加权(族序重排)+ lessons_refs 轻量检索(文件名/族头匹配) | language_issue_matrix.py |

## 可证伪判据（下一场战役逐条验）

- D-1：库型目标 R2 keep 率显著高于 QuickJS 的 0
- D-2：有近期安全修复史的目标，该通道产出独立假设且命中率高于面图通道
- D-3：含补强的波次 r35-collect 稳定回显 sibling 提示，漏接率归零
- D-4：hints 输出含该语言 lessons 引用；--kind 重排后目标族条目前置
任一判据证伪 → 裁除该杠杆，不心疼。

## 义务三问
①触发=目标为 git 仓库且有提交史(D-2)；其余随 R2/R3.5 流程；②消费者=假设生成/主代理裁决；③裁掉丢什么=对应战役观察的发现力（各判据已列）。
