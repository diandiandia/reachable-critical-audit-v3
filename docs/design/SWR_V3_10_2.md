# SWR_V3_10_2 — 软件需求（v3.10.2 缺陷修复）

> 对应文档：REQ_V3_10_2.md（需求语义）/ SOFTWARE_DESIGN_V3_10_2.md（改动点）。
> SWR 为可测契约：每条含断言式描述，测试实现见 test_v3102.py（16 测试）。
> 原则：旧队列零行为变化；新增行为全部有触发条件或缺省兼容。

## 1. 实证保真度（REQ-V3.10.2-001）

- **SWR-V3.10.2-001**：`empirical.fidelity` 合法值仅 `real_target|equivalent|mechanism`；缺省按 `real_target` 处理；非法值回退 `real_target` + 渲染 warn 行。
- **SWR-V3.10.2-002**：报告「实证记录（R5）」行在 `fidelity=equivalent` 时行首渲染 `等价复现:` 前缀；`real_target`（含缺省）无前缀；`mechanism` 行首渲染 `机制级:` 前缀。
- **SWR-V3.10.2-003**：`fidelity=mechanism` 的候选，`evidence_grade` 不得为 `empirically_confirmed`（回填时机械拦截，已回填的渲染 warn）。
- **SWR-V3.10.2-004**：gate ③ 判定与 fidelity 无关（判据不变）；PASS/FAIL 输出附 `fidelity_hint`（等价复现候选 id 列表，不改变判定）。

## 2. workflow 输入契约（REQ-V3.10.2-004）

- **SWR-V3.10.2-005**：导出脚本（四模式）的 agent 任务首步校验：默认契约键（`c.prompt`）或薄封装契约键（`c.taskFile`）至少其一非空；缺失 → 不派发 agent，返回模式对应错误结构（verify: `{id, verdict:"NEEDS_REVIEW", evidence:"workflow input missing: <key>"}`；refutation/resurrect 见软件方案 1.3）。
- **SWR-V3.10.2-006**：collect（含 r35/r35n）检测 journal 同 id 多 result 且内容 hash 各异 → stderr 输出 `journal_anomaly: <id> ×N distinct contents` 告警。

## 3. R4 簿记容错（REQ-V3.10.2-005）

- **SWR-V3.10.2-007**：r4-collect 对 finding/假说级 tracked 读取兼容 `surfaces` 别名（`tracked_surfaces = tracked_surfaces or surfaces`，幂等，不覆盖 canonical 键）。
- **SWR-V3.10.2-008**：空 findings 假说（verdict=reviewed_clean/not_applicable）声明全量扫掠 tracked 时不再触发 `R4_TRACKED_MISSING`；覆盖率簿记按扫掠面计；其他缺口（完全无 tracked）仍拦截。

## 4. 报告生命周期（REQ-V3.10.2-006/008/011）

- **SWR-V3.10.2-009**：`--stage report` 检测报告「三、修复建议与结论」段非占位符 → exit 1 + stderr「主代理段落已存在；--force 重生成将覆盖」；`--force` 重生成 + stderr 丢失告警。
- **SWR-V3.10.2-010**：报告渲染检查 `empirical.harness` 路径：非 `.audit_results/` 前缀（相对）或非项目根下（绝对）→ 该行尾渲染 `[产物目录违规 warn]`。
- **SWR-V3.10.2-011**：strengthened/attribution_correction 渲染默认附「（未复核）」；`refutation.*_verified_by == "main-agent"` 时去标记。

## 5. 队列生命周期（REQ-V3.10.2-007/003）

- **SWR-V3.10.2-012**：`--stage reopen --id <ID>`：仅 NEEDS_REVIEW 候选可重开；`reopen_reason` 必填（缺省拒绝）；重开后 status=PENDING，correction_record/needs_review_reason/refutation/resurrection_review 全保留。
- **SWR-V3.10.2-013**：NEEDS_REVIEW 成因规范化三档（`保守裁决|证据不足|环境受限`）；成因=`环境受限` 且 evidence 含上游公开佐证关键词时，附录 A 行渲染「佐证注记」列（佐证来源摘要 ≤120 字）。

## 6. 裁决与复核义务（REQ-V3.10.2-002/011）

- **SWR-V3.10.2-014**：correction_record 条目含 `demote_to` 且无 `adjudication_verification` → assert_ledger 输出 warn `adjudication_unverified: <id>`（不阻断）；有 → 无 warn。
- **SWR-V3.10.2-015**：REACHABLE 候选存在未签收补强（refutation.strengthened/attribution_correction 非空且无 `*_verified_by`）→ assert_ledger 输出 warn `strengthen_unverified: <id>`（不阻断）。

## 7. 平台与依赖知识（REQ-V3.10.2-010/012/009）

- **SWR-V3.10.2-016**：checklist_library `platform_trust_model` 清单族按平台键分派；条目为平台机制级描述，去项目化扫描（`_scan_runtime_assets`）0 命中；目标无对应平台组件时任务书零注入（零行为变化）。
- **SWR-V3.10.2-017**：verifier/refuter 步骤 1.5 的依赖对账可选步：仅在 R1 context 输出含 pinned 依赖清单时触发；产出 `dependency_cve_notes`（不改变 verdict）；报告附录 B 渲染「依赖风险注记」段。
- **SWR-V3.10.2-018**：`--stage workflow-script` 的 scope_changed 分支在 changed 非空时输出「受影响面重开建议」（物化目录 × R1 面路径交叉清单，建议级不阻断）；`write_scope_review` 落盘 append 至 `.audit_results/scope_review.jsonl`。

## 8. 实证模板（REQ-V3.10.2-013）

- **SWR-V3.10.2-019**：parser_fuzz 模板（及无匹配模板时的现场构造指引）头部含资源防护样板（按 lang 分派：进程限额/上限参数化），docstring 含「复现安全性：无防护环境勿跑极值预设」注记；c/cpp 语言手册对应小节含样板代码。

## 9. 版本与兼容

- **SWR-V3.10.2-020**：TOOLING_VERSION=3.10.2；SKILL.md v3.10.2 增量段齐备（数据模型速查含 fidelity/adjudication_verification/reopen_reason/成因三档）。
- **SWR-V3.10.2-021**：v3.10 及更早队列样本复跑：六门禁零新增告警、零误判（fidelity 缺省 real_target、无 reopen 字段、旧成因两档全部兼容）。
- **SWR-V3.10.2-022**：新增测试 test_v3102.py 全绿且既有测试全绿（≥271）；源仓库 `reachable-critical-audit-v3` 同步分 commit 提交 + 测试全绿。

## 10. 明确不建（回归护栏）

- 不新增终态、不改变 gate ③ 判据、不改变 verdict 枚举（SWR-V3.10.2 不触碰）。
- 不恢复 v3.10 维持撤销项（batch-size 提示、payload_hash 辅助）——无新失误案例，回归测试不加对应断言。
- severity override 不建新义务（现有 override+reason 可问责）。
