# SYSTEM_DESIGN_V3_12 — 状态机分析能力补强（STATE 族 + 状态机清单族 + 先例）

> 版本链：v3.11（2026-08-29，Android 系审计设计缺陷修复）→ **v3.12（2026-08-29，状态机分析能力补强）**。
> 能力增量版：不改变阶段骨架 R0-R6、不改变六门禁①-⑧判据语义、不改变队列数据模型。
> 背景动因：用户评估「skill 能否挖掘 状态机分析」——复杂文件解析/协议分析为成熟
> 主场（libvpx/Pillow/kafka/zookeeper 等战绩），状态机分析为灰色地带（LLM 临时
> 发挥有实战案例，缺族级机制兜底）。本版补齐家族级支持，全部走数据驱动与既有管线。

## 0. 第一原则自检（v3.3.2 义务入库三问 + 去项目化）

本版所有新义务逐条过三问（见 §3）。新资产全部为机制形态书写（零项目专属
API 名；项目名仅出现在 source_lessons 追溯字段）。零代码路径新增于绑定/聚合
主链（账本 CWE→族映射数据驱动、清单绑定走既有 binder、先例触达走既有 map
机制）。验收含 `_scan_runtime_assets` 去项目化扫描与「未审计过的新项目」场景。

## 1. 问题域（四缺口）

| 编号 | 问题 | 复盘案例（追溯） | 形态 |
|---|---|---|---|
| S-A | **覆盖账本无 STATE 族**：CWE-841/696/670 不在任何族 cwe 集——若 verifier 打标这三类状态机 CWE，聚合落 OTHER、无族级选题压力 | 全库 841/696/670 零出现（前瞻性锚点缺口）；历史状态机机制 finding 实际以资源维 CWE 落 RESOURCE-DOS（Pillow DCX/MPO=789、gson 无界 token=400，实证队列） | 账本缺口 |
| S-B | **严重度表无状态机 CWE**：841/696/670 不在 SEVERITY_BY_CWE → 841+claim=other 类候选走 medium 默认档（行为工作流绕过类误分级路径）；且 families 与分级表全族对齐惯例被打破 | 前瞻性误分级路径论证（非历史失真——DCX/MPO 经 789 已正确得 High） | 分级缺口 |
| S-C | **清单库仅一条状态机条目**：CK-BIZ-LOGIC 内「状态机越权」单步（业务逻辑信号驱动，非系统性）；转移/混淆/多步/门禁重入四形态无族级覆盖 | Pillow 复盘明示「多帧容器类目标值得用『逐帧状态机 vs 逐帧门禁』的对账清单」（SKILL_LESSONS_Pillow.md:16，逐字属实） | 知识供给缺口 |
| S-D | **先例库零状态机条目**：同类裁决（一次性门禁 vs 逐项重入）靠 agent 现场发挥——自证伪提示无弹药 | Pillow 教训未机制化为先例 | 知识供给缺口 |

## 2. 修复策略（5 项 REQ，详见 REQ_V3_12.md）

1. **覆盖账本 STATE 族**（S-A）：families 加 STATE（841/696/670）+ rows 空行；
   数据驱动 fam_map 自动生效（零代码改动），缺口扫描出现 STATE×16 语言格
2. **严重程度映射**（S-B）：841/696→高（与 RACE 同档）、670→中（恒错控制流默认）
3. **状态机族检查清单 4 条**（S-C）：转移显式拒绝（841 锚定）/双路径解释（关键词
   绑定，避免与 TRANSITION 双锚 841 机械共绑）/多步不变式（696 锚定）/逐项门禁
   重入对账（关键词+词边界信号门控）——详见 REQ-V3.12-003 绑定规则
4. **裁决先例**（S-D）：PREC-STATE-GATE-REENTRY 经 CWE 元组 + 关键词双路径触达
5. **版本链**（收口）：TOOLING_VERSION 3.12 + 文档四件套 + test_v312

## 3. 义务入库三问（新义务逐条）

| 新义务 | ①触发条件 | ②消费者 | ③案例支撑 |
|---|---|---|---|
| STATE 账本族 | 聚合/缺口读取（数据驱动，无新增代码路径） | 选题缺口格/报告 B.6/压力提示 | 841/696/670 全库零出现且无族锚点（前瞻性缺口） |
| 严重度 841/696/670 | `severity_for` cwe 命中 | 报告分组排序 | 841+claim=other 误入 medium 默认档的误分级路径；族全族入表惯例 |
| CK-STATE-TRANSITION | cwe 841 或 状态机/state machine 关键词 | verifier/refuter `_checklist_section` | CK-BIZ-LOGIC「状态机越权」步骤升级为族（v3.4 单步实战形态） |
| CK-STATE-CONFUSION | 重入/复用/双解释/状态混淆 关键词 | 同上 | Pillow 多帧 seek 逐帧改状态的复用面两实例 |
| CK-MULTISTEP-INVARIANT | cwe 696 或 多步/跳步/重放/乱序 关键词 | 同上 | CK-BIZ-LOGIC「多步流程不变式」步骤升级为族 |
| CK-FRAME-GATE-REENTRY | 关键词命中 **且** 容器/逐项信号命中（词边界门控） | 同上 | Pillow DCX/MPO 门禁时序 vs 状态机重入（复盘点名需求，实证队列 CWE-789） |
| PREC-STATE-GATE-REENTRY | cwe 元组 或 状态机 关键词（无 signals 拦截） | 自证伪提示段 | 同上（先例库零条目→现场发挥） |

## 4. 明确不做（义务棘轮防护）

- 不建 H8 状态机假说——gate ④ H1-H7 语义不变（假说体系改动过大，无漏报案例
  支撑族级假说；四形态已由清单族覆盖）。
- 不加通用状态序列 fuzz harness 模板——v3.6 B 裁决先例：8 语言通用模板已裁除
  （价值低）；状态机为项目专属形态，遵循「无匹配模板时现场构造」条款。
- 不加新门禁、不加新绑定维度、不注入 R4 biz_hypothesis 与 Mode A' 任务书。
- 不动 CK-BIZ-LOGIC 存量关键词（共绑定=跨族强化，合法）。
- 不收 CWE-799（交互频率语义属 RESOURCE-DOS 侧）。

## 5. 验收判据（Phase 3.12）

1. 全量回归全绿（301 基线 + test_v312 新增）+ 旧队列复跑零新增告警
2. 去项目化扫描 0 命中（新清单/新先例机制形态）
3. 覆盖账本 dry-run 列出 STATE×16 缺口格（选题压力可见）
4. 严重度/绑定/先例触达断言全绿（test_v312 覆盖负例：framework/statement 不误绑）
5. 未审计过的新项目验收：随下一在线项目自然触发（状态机清单绑定、STATE 格
   回填、先例提示至少各真实命中一次）
6. 源仓库同步分 commit + install + 安装版测试全绿
